# Rust Hello Goblin

This sample builds a Rust worker that reads JSON input/context files and writes
the standard Goblin result envelope.

```powershell
docker build -t goblin-example-hello-rust:local .
```
