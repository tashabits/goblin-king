# Ruby Hello Goblin

This sample is a tiny Ruby worker that reads the mounted contract files and
writes the standard Goblin result envelope.

```powershell
docker build -t goblin-example-hello-ruby:local .
```
