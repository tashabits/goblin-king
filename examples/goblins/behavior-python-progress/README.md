# Python Progress Behavior Goblin

Prints structured progress lines to stdout, reads input and context, and returns
step metrics in the result envelope.

```powershell
docker build -t goblin-example-behavior-python-progress:local .
```
