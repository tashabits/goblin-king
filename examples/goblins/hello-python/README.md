# Python Hello Goblin

This sample is the smallest Python baseline for the language-agnostic
container contract. It intentionally uses only the Python standard library and
does not import Goblin King internals.

```powershell
docker build -t goblin-example-hello-python:local .
```
