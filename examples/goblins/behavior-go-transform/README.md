# Go Transform Behavior Goblin

Reads `items` from input JSON, uppercases them, includes context metadata, and
returns transform metrics.

```powershell
docker build -t goblin-example-behavior-go-transform:local .
```
