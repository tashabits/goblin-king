# .NET Hello Goblin

This sample publishes a small .NET 8 worker image that implements the mounted
file contract and writes a Goblin result envelope.

```powershell
docker build -t goblin-example-hello-dotnet:local .
```
