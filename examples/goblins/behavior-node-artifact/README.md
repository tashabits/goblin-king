# Node Artifact Behavior Goblin

Writes a text artifact under `GOBLIN_ARTIFACT_ROOT`, returns artifact metadata,
and logs the artifact path to stdout.

```powershell
docker build -t goblin-example-behavior-node-artifact:local .
```
