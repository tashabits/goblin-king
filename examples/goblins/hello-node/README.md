# Node.js Hello Goblin

This sample proves that a JavaScript worker can follow the Goblin container
contract with no host-side Python imports.

```powershell
docker build -t goblin-example-hello-node:local .
```
