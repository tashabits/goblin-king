# Java Hello Goblin

This sample compiles a small Java 21 worker into a container. It validates the
mounted input/context files as JSON objects and writes a standard Goblin result.

```powershell
docker build -t goblin-example-hello-java:local .
```
