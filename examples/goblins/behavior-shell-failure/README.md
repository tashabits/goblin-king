# Shell Failure Behavior Goblin

Writes a failed Goblin result envelope and exits nonzero on purpose. This is a
controlled failure sample for runtime and admin testing.

```powershell
docker build -t goblin-example-behavior-shell-failure:local .
```
