# Goblin King Phase Proof Table

This table records the completed phase work and the remaining gaps. It is a maintainer
receipt, not a user guide.

## Phase Proof

| Phase | Scope | Met | Not Met / Outstanding |
| --- | --- | --- | --- |
| 1 | Library kernel | Python package skeleton, contracts, registry loading, in-process execution, SQLite persistence, CLI, example goblin, tests, and contribution guidance were added. | None known. |
| 2 | Scheduler kernel | Durable schedules, queued jobs, leases, retry/timeout fields, scheduler loop, CLI, and local-only test policy were added. | None known. |
| 3 | Docker execution | Docker runtime, per-worker Dockerfiles, worker image map, Redis result transport, Compose assets, Docker-first docs, and Docker proof were added. | None known. |
| 4 | FastAPI control plane | API settings, auth token, jobs/schedules/runs/artifacts endpoints, API CLI/Compose support, tests, and API roadmap were added. | Later API surfaces were intentionally deferred at the time and later covered. |
| 5 | Reusable package integration | Project settings, multiple registries, entry point discovery, template generator, CLI/API integration, and package reuse docs were added. | Public PyPI publication remains out of scope. |
| 6 | Fanout and retry APIs | Durable fanouts, job metadata, fanout/retry services, API endpoints, CLI commands, tests, and docs were added. | None known. |
| 7 | Events, streaming, and heartbeats | SQLite event log, Redis pub/sub, WebSocket run stream, scheduler/worker heartbeats, API/CLI commands, worker heartbeat contract, and docs were added. | None known. |
| 8 | Production API hardening | Local users, teams, projects, memberships, hashed API tokens, project scoping, audit logs, rate limits, pagination, OpenAPI contracts, and tests were added. | External identity beyond generic OIDC/JWT remains out of scope. |
| 9 | Optional Kubernetes and admin proof | Optional Helm deployment, FastAPI-served admin proof path, long-running service support, hello/sample goblins, ingress notes, and user guide were added. | Kubernetes remains optional, not the default path. |
| 10 | React admin tester interface | Separate React/Vite admin service, Docker/Helm routing, admin panels for current control-plane paths, long-service controls, cleanup controls, screenshots, and docs were added. | Hard container/pod termination was later strengthened in Phase 20. |
| 11 | Stable internal package boundary | Public root imports, public/semi-public/internal module guidance, internal wheel compatibility policy, and adoption guide were added. | Public PyPI hardening remains out of scope. |
| 12 | Project plugin SDK | Generated plugin SDK path, worker folder templates, entry point metadata, validation commands, tests, and docs were added. | Official SDKs for every language remain out of scope. |
| 13 | Deploy-time discovery reload | Runtime reload for registry/project/image sources, admin discovery endpoints, scheduler refresh behavior, admin Discovery panel, and reload tests were added. | None known. |
| 14 | Host project deployment integration | Docker Compose extension fixture, Helm values pattern, project build/reload/proof commands, and admin-visible host-project goblin proof were added. | Cloud-specific deployment recipes remain out of scope. |
| 15 | Project-ready release and upgrade | Release checklist, compatibility matrix, upgrade guide, migration guide, first-hour guide, wheel/adoption proof, and fixture tests were added. | Public package distribution remains out of scope. |
| 16 | Production Kubernetes hardening | Helm resources, HPA, PDB, service accounts/RBAC, network policies, ingress TLS/options, PVC settings, and existing-secret support were added. | Managed ingress/storage/secret-provider recipes remain deployment-specific. |
| 17 | Redis Streams durable delivery | Redis Streams alongside pub/sub and SQLite events, stream status/read APIs, CLI/admin visibility, tests, and docs were added. | Redis remains live/replay transport; SQLite remains durable truth. |
| 18 | OIDC authentication | OIDC/JWT bearer validation, JWKS cache settings, claim mapping, local-token precedence, RBAC regression tests, and docs were added. | Provider-specific login flows remain out of scope. |
| 19 | Volume-backed artifact management | Artifact storage health/status, cleanup dry-run/execution, max-age/max-byte policy, admin artifact management UI, Compose volume and Helm PVC docs were added. | Object storage providers remain out of scope. |
| 20 | Scoped runtime termination | Docker container IDs/labels, Kubernetes Job/Pod identifiers, API/CLI/admin hard-kill controls, audit/events, and safe no-op behavior were added. | Runtime kill is scoped only to Goblin King-owned objects. |
| 21 | Image promotion and deployment orchestration | Promotion records, build/push/mark flows, deployment records, Helm render intent, discovery reload proof trail, CLI/admin panels, and tests were added. | Registry-specific promotion automation remains out of scope. |
| 22 | Production roadmap closeout | Roadmap/docs/screenshots were audited and closeout docs captured proof surfaces and deferred items. | Later phases added more language/runtime work after this closeout. |
| 23 | Repo-wide code cleanup | Baseline release `v0.1.0-alpha.0` was created, backend JSON/metadata helpers were introduced, SQLite schema/migration/row mapping was split, API/runtime/CLI support helpers were extracted, React admin shell/Goblin Lab/Task Board panels were split, shared API test setup helpers were added, cleanup docs were refreshed, and every staged PR preserved behavior with full local CI plus Docker/Helm admin runtime audits. | Ongoing cleanup should continue as new features land; completion release `v0.1.0-alpha.1` records this cleanup pass. |
| 24 | Formal Goblin Container Contract | Canonical language-agnostic container contract was documented, including inputs, context, results, artifacts, events, heartbeats, exit codes, and WASI wrapper model. | Native Kubernetes WASI scheduling remains out of scope. |
| 25 | Goblin authoring documentation | Human-facing author docs, Dockerfile guidance, security model, and language-agnostic worker guidance were added. | Official language SDKs remain out of scope. |
| 26 | Minimal cross-language hello goblins | Hello workers for .NET, Go, Java, Node.js, PHP, Python, Ruby, Rust, shell, and supporting tests/docs were added. | None known. |
| 27 | Container-wrapped WASI goblins | C/WASI and Rust/WASI examples running through Wasmtime in containers were added with tests/docs. | Native host-level WASI runtime remains out of scope. |
| 28 | Cross-language runtime proof | Cross-language registry/image map, Docker runtime proof target, API visibility test, and docs were added. | These goblins were separate from the default admin registry until the later demo-registry update. |
| 29 | Cross-language contract behaviors | Artifact, progress, cancellable, transform, failure, and WASI context behavior samples were added with tests and proof targets. | None known. |
| 30 | Goblin contract validation | `goblin-king workers validate`, validation docs, tests, and Makefile validation targets were added. | Deep conformance certification beyond practical validation remains out of scope. |
| 31 | Container-first admin/docs wording | Admin UI and docs were updated to present goblins as language-agnostic OCI worker containers. | None known. |
| 32 | Language-agnostic closeout | Language/runtime docs and samples were audited; closeout doc captured proof commands and deferred non-goals. | Deferred items are tracked below. |
| 33 | Per-goblin resource policies | `docs/goblin-resource-policies.md` documented policy shape, defaults, ceilings, Docker mapping, Kubernetes/Helm mapping, and proof expectations. | Runtime enforcement was intentionally completed in Phase 34. |
| Preflight | Roadmap proof hardening | `roadmap-proof-preflight` merged the proof-table, roadmap, README/user-doc audit, admin runtime audit helper/doc, and Docker/Helm audit expectations. | None known. |
| 34 | Runtime resource policy enforcement | `goblin-resource-policies.json`, policy loading, defaults/overrides/ceilings, queue-time rejection, persisted job/run effective policy metadata, API/CLI/scheduler integration, Docker runtime flags, Kubernetes Job resource fields, artifact count/byte checks where artifacts are inspectable, concurrency deferral, events, audit records, Helm config, admin run visibility, and focused tests were added. | Secret allow-lists, provider-specific admission controls, object-storage quota enforcement, and deeper policy engines remain deferred. |
| 35 | Project-adoptable goblin configuration | Versioned `GoblinProject` config, inline container goblin definitions, path resolution, safe secret references, registry/image-map merging, API/admin `project-config` source reporting, adopting-project fixture updates, tests, docs, full local CI, project validation/list smoke proof, Docker/Helm admin audits, and screenshots at `docs/screenshots/phase-35-docker-admin.png` and `docs/screenshots/phase-35-helm-admin.png` were added. | None known for this phase. |
| 36 | Bring-your-own-goblin validation | `workers validate --project`, `workers validate-image`, prebuilt image checks, project-config validation dispatch, missing result JSON proof, invalid result JSON proof, missing image proof, tests, docs, Docker/Helm admin audits, and screenshots at `docs/screenshots/phase-36-docker-admin.png` and `docs/screenshots/phase-36-helm-admin.png` were added. | Deeper conformance certification remains deferred beyond practical local validation. |
| 37 | Project template and quickstart | `goblin-king project init`, standalone adopter project template, hello and artifact worker folders, input files, schemas, quickstart docs, tests, local validation proof, Docker/Helm admin audits, and screenshots at `docs/screenshots/phase-37-docker-admin.png` and `docs/screenshots/phase-37-helm-admin.png` were added. | None known for this phase. |
| 38 | External project scheduling and run inspection | Project-aware `jobs submit`, project-aware `schedules add`, source metadata on queued/materialized jobs, API source metadata, `runs show --with-job`, scheduler metadata tests, project-defined Docker smoke proof, full Docker/Helm admin audits, and screenshots at `docs/screenshots/phase-38-docker-admin.png` and `docs/screenshots/phase-38-helm-admin.png` were added. | None known for this phase. |
| 39 | v1alpha1 contract and public boundaries | Public root version constants, `GOBLIN_CONTRACT_VERSION` runtime env for Docker/Kubernetes workers, named `goblin-king/v1alpha1` compatibility matrix entries, contract/config/public API docs, project config validation, focused tests, full Docker/Helm admin audits, and screenshots at `docs/screenshots/phase-39-docker-admin.png` and `docs/screenshots/phase-39-helm-admin.png` were added. | None known for this phase. |
| 40 | Adopter documentation pass | README adopter section, `docs/adopter-guide.md`, project-config-first wording in `docs/ADOPTING_PROJECTS.md`, README heading/link documentation tests, full Docker/Helm admin audits, screenshots at `docs/screenshots/phase-40-docker-admin.png` and `docs/screenshots/phase-40-helm-admin.png`, and updated roadmap/proof docs were added. | None known for this phase. |
| 41 | Adopter smoke suite | `goblin-king smoke adopter-project`, `make adopter-smoke`, generated adopter project validation, project goblin scheduling, success/artifact/controlled-failure inspection, cleanup, tests, docs, full local CI, clean-board Docker/Helm admin audits, explicit WASI launches from the admin console, and screenshots at `docs/screenshots/phase-41-docker-admin.png`, `docs/screenshots/phase-41-helm-admin.png`, and `docs/screenshots/phase-41-admin-wasi-ui.png` were added. | None known for this phase. |
| 42 | Project-adoptable alpha closeout | README status updated to `Project-adoptable alpha`, clean-checkout adopter smoke passed, branch-local adopter smoke passed, docs were re-audited for Python-worker assumptions, proof/roadmap/outstanding items were updated, Docker/Helm admin audits passed for every registered goblin including WASI workers, and screenshots at `docs/screenshots/phase-42-docker-admin.png` and `docs/screenshots/phase-42-helm-admin.png` were added. | None known for the project-adoptable alpha milestone. |
| 43 | Slim package root public API | Package root import was made lightweight for adopter code; heavy API, scheduler, and store objects are available through explicit modules and lazy compatibility shims; import-hygiene tests, public API docs, full local CI, Docker admin audit, and Helm render/admin audit were completed. | None known for this phase. |
| 44 | Behavior-preserving module cleanup pass | API state/discovery loading moved to `api_state.py`; runtime termination and resource-policy API bookkeeping moved to `api_runtime.py`; public imports, CLI commands, API routes, response models, runtime behavior, scheduler behavior, database behavior, and admin UX were preserved; full local CI, Docker admin audit, and Helm render/admin audit were completed. | None known for this phase. |
| 45 | Mandatory goblin validation gate | Worker validation records are persisted with kind, image, resolved image identity, contract version, validator version, timestamp, status, failure reasons, and effective policy; CLI validation persists proof and exposes `workers validation-status`; API goblin listing exposes latest validation status; the scheduler validates container images before execution and records failed validation as failed runs plus audit/event proof; validation lookup is tied to the current image digest so changed images require fresh proof. | Local proof completed on the Phase 45 branch: `python -m pytest` passed 149 tests, `python -m ruff check .` passed, Docker admin audit spawned every registered goblin including WASI and long-running service proof, Helm render passed, Helm admin audit spawned every registered goblin, and CLI validation-status showed persisted digest/contract/validator evidence. |
| 46 | Docker resource policy proof | Added `resource-policies inspect` to show effective policy, Docker arguments, Kubernetes container fields, artifact ceilings, and log ceilings from one policy file; added `examples/resource-policy-proof.json` to demonstrate CPU, memory, PID, network-disabled mode, read-only root, tmpfs, artifact limits, log limits, timeout, retry, and concurrency settings; fixed policy merging so non-empty default tmpfs mounts are not erased by empty per-goblin overrides; extended tests for Docker/Kubernetes mapping and above-ceiling rejection across supported policy fields. | Local proof completed on the Phase 46 branch: `python -m pytest` passed 150 tests, `python -m ruff check .` passed, `resource-policies inspect` showed Docker/Kubernetes mappings, `make docker-up` rebuilt the stack, `make helm-template` rendered, `make helm-up` deployed revision 19, and Docker/Helm admin audits spawned every registered goblin type including WASI and long-running service proof. |
| 47 | Project config hero path | Project config is now the primary documented adoption path in README, adopter guide, project config docs, generated project README, and project template quickstart; the golden path covers project init, config validation, goblin listing, worker validation/revalidation status, job submit, run inspection, artifact-producing run inspection, schedule creation, and scheduler execution; adopter smoke persists validation records before scheduling so project-defined goblins follow the mandatory validation gate flow. | Local proof completed on the Phase 47 branch: `python -m pytest` passed 150 tests, `python -m ruff check .` passed, focused smoke/template/project/CLI tests passed, `smoke adopter-project` validated/scheduled `phase47.hello`, `phase47.artifact`, and `phase47.failure`, Docker stack rebuilt, Helm rendered and deployed revision 20, and Docker/Helm admin audits spawned every registered goblin type including WASI and long-running service proof. |
| 48 | Adoption hardening closeout | Re-audited README, roadmap, adopter docs, proof table, and closeout docs after Phases 43-47; confirmed README status is `Project-adoptable alpha`; confirmed docs state unvalidated goblins are not schedulable by default; left only explicit deferred items for public PyPI, untrusted multi-tenant hardening, signing/scanning, cloud-specific recipes, object storage beyond volume/PVCs, native WASI scheduling, deeper certification, and official language SDKs. | Local proof completed on the Phase 48 branch: `python -m pytest` passed 150 tests, `python -m ruff check .` passed, `smoke adopter-project` validated/scheduled `phase48.hello`, `phase48.artifact`, and `phase48.failure`, Helm rendered, Docker and Helm admin audits spawned every registered goblin type including WASI and long-running service proof, and stale planned/outstanding roadmap language was removed. |
| 49 | Project resource defaults | `GoblinProject` config now supports `defaults.resources`; project loading deep-merges those defaults into inline goblin `resources`, validates the project default and effective per-goblin resources against local policy ceilings when present, layers project defaults into effective API/CLI job policy when a project is supplied, and exposes the default block in project validation/discovery proof for operator visibility. README, project config docs, resource policy docs, adopter guide, template quickstart, and roadmap were updated to explain when to use project defaults versus the runtime policy file. | Local proof completed on the Phase 49 branch: `python -m pytest` passed 161 tests, `python -m ruff check .` passed, admin tests/build passed, `project validate` printed `defaults.resources`, Docker job submit persisted inherited timeout/memory policy, above-ceiling defaults were rejected clearly, Helm rendered, and Docker/Helm admin audits spawned every registered goblin type including WASI and long-running service proof. |
| 50 | Goblin resource overrides and effective policy resolution | Effective resource policy resolution is now formalized as operator defaults plus project defaults plus goblin-specific overrides. Resource policy models reject unknown top-level and nested fields, inline project goblin resources layer into runtime policy sets, ceiling checks validate final effective policies, persisted job/run records expose `effective_policy`, and the React admin task board/run detail panels show formatted effective policy JSON. README, project config docs, resource policy docs, and roadmap were updated with the layered rule. | Local proof completed on the Phase 50 branch: `python -m pytest` passed 167 tests, `python -m ruff check .` passed, admin tests/build passed, `make helm-template` rendered, Docker CLI proof showed project defaults plus goblin override persisted as timeout `90`, memory `1Gi`, and inherited artifact file cap `2`, above-ceiling memory was rejected, `make docker-up` rebuilt the stack, `make helm-up` deployed revision 21, and Docker/Helm admin audits spawned every registered goblin type including WASI and long-running service proof. |
| 51 | Project and goblin concurrency limits | `concurrency.max_running` remains the per-kind active-job cap and `concurrency.max_project_running` now caps active leased/running jobs within one project scope. Scheduler deferral keeps over-cap jobs queued, records a visible `last_error`, and emits `resource_policy.concurrency_deferred` events with scope, active count, limit, and reason. API/CLI/admin job surfaces expose the existing queued status and deferral reason. README, resource policy docs, project config docs, and roadmap were updated. | Local proof completed on the Phase 51 branch: `python -m pytest` passed 169 tests, `python -m ruff check .` passed, admin tests/build passed, Docker scheduler CLI proof deferred both project-wide and per-kind over-cap jobs with queued status plus events, `make helm-template` rendered, `make docker-up` rebuilt the stack, `make helm-up` deployed revision 22, and Docker/Helm admin audits spawned every registered goblin type including WASI and long-running service proof. |
| 52 | Effective policy runtime mapping | Docker and Kubernetes runtimes now expose the exact resolved effective policy to workers through `GOBLIN_EFFECTIVE_RESOURCE_POLICY_JSON`; Docker still maps supported CPU, memory, PID, network, read-only root, tmpfs, and log fields to `docker run` flags; Kubernetes still maps CPU/memory resources, read-only root filesystem, and active deadlines into Job specs; Helm default policy values include project-wide concurrency caps; the resource policy docs include a Goblin King/Docker/Kubernetes responsibility matrix. | Local proof completed on the Phase 52 branch: `python -m pytest` passed 169 tests, `python -m ruff check .` passed, admin tests/build passed, `resource-policies inspect` printed Docker args and Kubernetes fields for CPU/memory/PID/network/read-only/tmpfs/log policy, `make helm-template` rendered `max_project_running`, `make docker-up` rebuilt the stack, `make helm-up` deployed revision 23, and Docker/Helm admin audits spawned every registered goblin type including WASI and long-running service proof. |
| 53 | Resource policy documentation and closeout | README, `docs/goblin-resource-policies.md`, adopter guide, project template quickstart, project-adoptable roadmap, production closeout, and this proof table now describe the completed layered resource-policy model: operator defaults and ceilings, project `defaults.resources`, goblin overrides, effective policy persistence, project-wide concurrency deferral, runtime policy environment, Docker/Kubernetes mapping, and the remaining deployment-specific gaps. | Local proof completed on the Phase 53 branch: `python -m pytest` passed 169 tests, `python -m ruff check .` passed, admin tests passed 7 tests, admin build passed, `resource-policies inspect` printed effective policy plus Docker/Kubernetes mappings, `project validate` printed adopting-project coverage, `make helm-template` rendered the layered policy config, `make docker-up` rebuilt the stack, `make helm-up` deployed revision 24, and Docker/Helm admin audits spawned every registered goblin type including WASI and long-running service proof. Release `v0.1.0-alpha.2` was created from synced `main` after the PR merged. |
| 54 | Vendored project usage docs | Added `docs/using-goblin-king-from-a-vendored-checkout.md` and linked it from README, adopter guide, project scheduler guide, and project template quickstart. The guide covers submodule, subtree, and local path dependency adoption while preserving the CLI/project-config workflow and container-first worker model. | Local proof completed on the Phase 54 branch: docs tests, ruff, command-shape proof, Helm render proof, and Docker adopter validation proof passed. |
| 55 | Adopter admin dev/test stack | Added `docs/adopter-admin-dev-stack.md` and linked it from README, adopter guide, project scheduler guide, vendored checkout guide, admin guide, and security model. The guide documents the Compose host-project stack, discovery reload, worker validation, CLI submission, admin inspection, shutdown, and Docker socket boundaries. | Local proof completed on the Phase 55 branch: docs tests, ruff, admin tests/build, Helm render proof, Docker Compose config proof, project validation, worker validation, and admin API proof passed. |
| 56 | Admin project goblin visibility | The React admin registered goblin table now visually marks project-config goblins, shows validation status messages inline, and includes the real `workers validate` repair command for unknown, stale, or failed validation states. Existing job effective-policy, run result, artifact, source, image, and worker mapping surfaces remain unchanged. | Local proof completed on the Phase 56 branch: docs tests, ruff, admin tests/build, Helm render proof, Docker Compose host-project stack proof, discovery reload, and admin API project goblin proof passed. |
| Cleanup | Mandatory validation gate documentation | `docs/goblin-contract-validation.md` was made the canonical design doc for the scheduler validation gate, README/adopter/contract/project/security docs now link back to it, scheduler validation failures now include actionable repair guidance, `/goblins` exposes a friendly validation status payload, and the React admin shows validated/failed/stale/unknown badges for registered goblins. | Proof belongs to PR `Document mandatory goblin validation gate`: local CI, admin build/tests, Docker validation proof, and Helm render/admin proof. |

## Deferred Items

- Current release: `v0.1.0-alpha.2`.
- Secret allow-lists, provider-specific admission controls, object-storage quota
  enforcement, and deeper resource-policy engines.
- Public PyPI/package-publication hardening.
- Cloud-provider-specific managed service recipes.
- Object storage providers beyond Docker volumes and Kubernetes PVCs.
- Native Kubernetes WASI scheduling and host-level Wasm runtime support.
- Official language SDKs beyond the current language-agnostic container contract and
  examples.
- Deep goblin conformance certification beyond the practical local validation command.
- Unvalidated goblins are not schedulable by default; current validation proof is keyed
  to the resolved image identity, contract version, and validator version.
